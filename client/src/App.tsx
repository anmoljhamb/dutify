import { Button } from "@mui/material";

const App = () => {
    return (
        <div className="justitfy-center flex h-screen w-screen flex-col items-center justify-center bg-black">
            <h1 className="text-4xl uppercase text-white">hello world!</h1>
            <Button>Login???</Button>
        </div>
    );
};

export default App;
